/*1. Variable:
- let: let sẽ được khi nó thay đổi giá trị.
- const: const sẽ được dùng khi nó không thể thay đổi giá trị.

2.Số ngẫu nhiên: Math.random()
*/

const fullName = "Tran Minh Thanh"; // Cannot change the value of fullName.
let number = 20; // Can change the value of number.
const age = Math.random();
